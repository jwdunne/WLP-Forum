 and asm auto bitand bitor bool break case catch char class compl const const_cast
 continue default delete do double dynamic_cast else enum explicit export extern
 false float for friend goto if inline int long mutable namespace new not operator
 or or_eq private protected public register reinterpret_cast return short signed
 sizeof static static_cast struct switch template this throw true try typedef typeid
 typename  union unsigned using virtual void volatile wchar_t while xor xor_eq

//this is a test_ ! ? .
/// another test
/*test again*/
/* comment */
/**	comment	 **/
/** this line is a comment
*guess what?
	*	another test
* jus t to *#?/
*stres$ Tes_T. !
commented ! **/
int main () {
MessageBox(NULL, "this is a string", "This is another string", MB_OK | MB_ICONERROR);
}
return 0;
!= == = >= <=
&& ||
!, =, +, -, *, /, %, <, >, <=, >=, +=, -=, *=, /=,
%=, >>=, <<=, &=, ^=, |=, ==, !=, ||, &&, ++, --, ? :,
&, |, ^, ~, <<, >>

[]
char mchar[MAX_CHAR]; adfja;lk
[1024]
$var
$var = 2
if (($var == $MyVar01) || if ($var = $adf)) {
	$var = 2;
	return ($var)
}
$_MyVar01